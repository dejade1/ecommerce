# ==================== BACKEND ENVIRONMENT VARIABLES ====================
# Copia este archivo a .env y configura los valores reales
# ⚠️ NUNCA comitear el archivo .env al repositorio

# ==================== SERVER ====================
PORT=3000
NODE_ENV=development  # development | production

# ==================== DATABASE ====================
# Para desarrollo (SQLite)
DATABASE_URL="file:./prisma/dev.db"

# Para producción (PostgreSQL recomendado)
# DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# ==================== JWT SECRETS (OBLIGATORIO) ====================
# ⚠️ CRÍTICO: Estos valores SON OBLIGATORIOS en producción
# Generar con: openssl rand -base64 32

JWT_SECRET=your-super-secret-jwt-key-CHANGE-THIS-min-32-chars
JWT_REFRESH_SECRET=your-refresh-secret-CHANGE-THIS-min-32-chars

# ==================== FRONTEND ====================
FRONTEND_URL=http://localhost:5173

# ==================== HARDWARE (OPCIONAL) ====================
# IP del ESP32/Arduino para control de LEDs
ESP32_IP=192.168.18.14

# ==================== EMAIL (OPCIONAL) ====================
# Para notificaciones de lotes próximos a vencer
RESEND_API_KEY=your-resend-api-key
ADMIN_EMAIL=admin@yourdomain.com

# ==================== LOGGING (OPCIONAL) ====================
LOG_LEVEL=info  # debug | info | warn | error

# ==================== NOTAS ====================
# 1. Generar secretos seguros:
#    openssl rand -base64 32
#
# 2. Para producción, usar PostgreSQL:
#    DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
#
# 3. Configurar CORS correctamente:
#    FRONTEND_URL=https://yourdomain.com
#
# 4. Habilitar HTTPS en producción (NODE_ENV=production)
